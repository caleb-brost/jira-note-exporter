# Jira Note Exporter

A Python tool that extracts Jira task info from Outlook assignment emails and generates Obsidian-friendly Markdown notes.
Includes links, key metadata, and a customizable template to keep sprint tasks organized and accessible.

---

## 🚀 Setup Instructions

1. **Install Required Libraries**

   ```bash
   pip install -r requirements.txt
   ```

2. **Create Your Config File**

   ```bash
   cp config_template.py config.py
   ```

3. **Edit `config.py`**
   Add your credentials and file paths as needed.

